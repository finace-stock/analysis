name: Build Image

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
      - name: Build Image
        env:
          DB_SECRET: ${{ secrets.DB_SECRET }}
        run: |
          docker build -t hightall/stock-analysis .